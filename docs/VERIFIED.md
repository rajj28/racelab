
# Verified environment

Generated by `python spike/verify_env.py --write`. Every statement below was read from a live cluster rather than from documentation or a version number, because defaults have changed across releases and managed plans restrict operations that the open-source documentation does not mention.

_Last run: 2026-08-14 16:09 UTC_

## CockroachDB

```
CockroachDB CCL v26.2.5 (x86_64-pc-linux-gnu, built 2026/07/28 18:56:00, go1.25.5)
```

### What is the default transaction isolation level?

**OK** — `serializable` — this is the default an application gets with no configuration.

### What is the actual gc.ttlseconds on this cluster?

**OK** — `gc.ttlseconds = 4500` (1.2 h). Every `AS OF SYSTEM TIME` read in the demo must be newer than this, so the UI should read seconds-old timestamps, never hours-old ones.

```
ALTER RANGE default CONFIGURE ZONE USING
	range_min_bytes = 134217728,
	range_max_bytes = 536870912,
	gc.ttlseconds = 4500,
	num_replicas = 3,
	num_voters = 3,
	constraints = '{+region=aws-ap-south-1: 3}',
	voter_constraints = '[]',
	lease_preferences = '[[+region=aws-ap-south-1]]'
```

### Is `ALTER TABLE ... CONFIGURE ZONE USING gc.ttlseconds` permitted on this plan?

**OK** — Permitted. We can widen the GC window for historical reads if needed, though the demo should stay well inside the default window regardless.

```
ALTER TABLE racelab_probe_vec_zone CONFIGURE ZONE USING
	range_min_bytes = 134217728,
	range_max_bytes = 536870912,
	gc.ttlseconds = 90000,
	num_replicas = 3,
	num_voters = 3,
	constraints = '{+region=aws-ap-south-1: 1}',
	voter_constraints = '[+region=aws-ap-south-1]',
	lease_preferences = '[[+region=aws-ap-south-1]]'
```

### Is `feature.vector_index.enabled` already true?

**OK** — Reported `True`. No SET required.

### Can we SET `feature.vector_index.enabled` on this plan?

**OK** — The SET succeeded. Setup can enable it idempotently.

### Does `CREATE VECTOR INDEX` work with `vector_l2_ops` (L2, `<->`) at VECTOR(1024)?

**OK** — Index created and accepted a VECTOR(1024) column. The query plan for `ORDER BY embedding <-> ...` references the vector index, so retrieval is index-backed rather than a full scan.

```
distribution: local

• top-k
│ order: +column8
│ k: 5
│
└── • render
    │
    └── • lookup join
        │ table: racelab_probe_vec@racelab_probe_vec_pkey
        │ equality: (id) = (id)
        │ equality cols are key
        │
        └── • vector search
              table: racelab_probe_vec@racelab_probe_vec_idx
              target count: 5
```

### Does `CREATE VECTOR INDEX` work with `vector_cosine_ops` (cosine, `<=>`) at VECTOR(1024)?

**OK** — Index created and accepted a VECTOR(1024) column. The query plan for `ORDER BY embedding <=> ...` references the vector index, so retrieval is index-backed rather than a full scan.

```
distribution: local

• top-k
│ order: +column8
│ k: 5
│
└── • render
    │
    └── • lookup join
        │ table: racelab_probe_vec@racelab_probe_vec_pkey
        │ equality: (id) = (id)
        │ equality cols are key
        │
        └── • vector search
              table: racelab_probe_vec@racelab_probe_vec_idx
              target count: 5
```

### Does `AS OF SYSTEM TIME` return the pre-write state (needed for the historical-evidence view)?

**OK** — Historical read saw 0 row(s); current read saw 1. The historical read reflects the state before the write, which is what the UI needs to show the state a losing transaction reasoned over.

## PostgreSQL

```
PostgreSQL 16.12, compiled by Visual C++ build 1944, 64-bit
```

### What is the default transaction isolation level?

**OK** — `read committed` — this is the default an application gets with no configuration, and it is what Arm A runs under. PostgreSQL also offers SERIALIZABLE; the comparison in this project is between default isolation behaviours.

### Is this PostgreSQL 16?

**OK** — Reported server_version `16.12`.
